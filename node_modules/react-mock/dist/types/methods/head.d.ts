import Pretender, { ResponseData } from 'pretender';
import { IDataGenerator } from '../data-generator';
/**
 * @param dg
 * @param endPoint
 * @param handler
 * @param others
 */
export declare function createHeadRoute(dg: IDataGenerator, endPoint: string, handler: (req: Object, generator: IDataGenerator) => ResponseData | Promise<ResponseData>, ...others: Array<any>): (this: Pretender) => void;
export default createHeadRoute;
